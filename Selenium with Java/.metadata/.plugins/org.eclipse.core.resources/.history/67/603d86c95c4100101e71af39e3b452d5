package First;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.edge.EdgeDriver;

public class First {
	WebDriver driver;
	
	//launching edge
	public void LaunchBrowser() {
		System.setProperty("webdriver.msedge.driver","D:/Projects/Installables/edgedriver_win64/msedgedriver.exe");
		driver = new EdgeDriver();
		driver.get("https://www.simplilearn.com/");
	}
	//searching for selenium course and clicking on it
	public void search() {
		driver.findElement(By.id("header_srch")).sendKeys("selenium");
		driver.findElement(By.xpath("//*[@id=\"homepage-search-form\"]/form/button[2]/span")).click();
		driver.findElement(By.xpath("//h4[text()='Automation Testing Masters Program']")).click();
		System.out.println("The Page Title is- "+driver.getTitle());
	
	}
	//CLosing the Browser
	public void close() {
		driver.quit();
	}
	
	public static void main(String[] args) {
		First obj= new First();
		obj.LaunchBrowser();
		obj.search();
		obj.close();

	}

}
